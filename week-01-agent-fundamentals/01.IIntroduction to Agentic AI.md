# Lecture Notes: Introduction to Agentic AI & Autonomous Agents

**Course:** AI Engineer Agentic Track (Week 1, Day 1)  
**Topic:** Foundations of Agentic AI, Autonomous Execution, and Tool Calling  
**Delivery Method:** Interactive Demonstration & Conceptual Overview  

---

## Executive Summary

This lecture establishes the foundational framework for **Agentic AI**. It shifts the software development paradigm from traditional single-turn Large Language Model (LLM) interactions (e.g., standard prompt-response chats) to **autonomous agents** capable of executing loops, reasoning dynamically, and interacting with external environments via executable tools.

---

## System Architecture

```mermaid
graph TD
    User([User Prompt]) --> Agent[AI Agent Core Loop]
    
    subgraph Agent Architecture
        Agent <--> Memory[(Simple Memory)]
        Agent <--> Model[LLM Engine: GPT-5.4 Mini]
    end
    
    Agent -->|Dynamic Tool Execution| Tools{Tool Orchestration}
    
    Tools -->|Read Market Data| API[MarketStack API]
    Tools -->|Read Portfolio Rows| SheetRead[Google Sheets API: Read]
    Tools -->|Append New Trade| SheetWrite[Google Sheets API: Write]

```
## 3. Practical Architecture Demonstration: Portfolio Balancing Agent

To demonstrate true agent autonomy, an n8n agentic workflow was constructed using **three discrete tools**:

1. **MarketStack API Tool (Read):** Fetches real-time stock price quotes for any stock ticker symbol.
2. **Google Sheets Read Tool (Read):** Queries a target spreadsheet (`Investments`) to retrieve existing holdings (e.g., 10 shares of Amazon, 2 shares of Apple).
3. **Google Sheets Write Tool (Write):** Appends new rows to the spreadsheet, using dynamic parameter generation by the LLM (Ticker symbol and Share quantity).

---

## 4. Execution Step Walkthrough & Decision Logic

```mermaid
sequenceDiagram
    autonumber
    actor User
    participant Agent as AI Agent (GPT-5.4)
    participant Sheets as Google Sheets API
    participant Market as MarketStack API

    Note over User, Market: Scenario 1: Information Retrieval & Calculation
    User->>Agent: "What's the value of my stock portfolio?"
    Agent->>Sheets: Call Read Tool (Fetch Holdings)
    Sheets-->>Agent: Returns AMZN (10) & AAPL (2)
    Agent->>Market: Call Market Quote Tool (AMZN, AAPL)
    Market-->>Agent: Returns latest stock prices
    Agent->>User: Returns computed total portfolio value

    Note over User, Market: Scenario 2: Autonomous Decision & Action Execution
    User->>Agent: "Suggest an investment to balance portfolio and add it to my sheet."
    Agent->>Agent: Analyzes portfolio (High Tech-concentration)
    Agent->>Agent: Selects diversification strategy (SPY / BND)
    Agent->>Sheets: Call Write Tool (Append 1 share of SPY)
    Sheets-->>Agent: Confirmation of row insertion
    Agent->>User: Confirms trade addition and explains reasoning
```
